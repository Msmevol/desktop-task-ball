# Corporate network safety nets. Harmless if unused.

# Relax SSL for downstream scripts in corporate MITM environments.
strict-ssl=false

# electron's postinstall downloads the Electron binary. Route it through
# a public mirror that's reachable from most corporate networks.
electron_mirror=https://npmmirror.com/mirrors/electron/
